{
  "name": "centipede",
  "version": "0.1.0",
  "description": "A faithful, from-scratch, browser-based recreation of the 1981 Atari arcade game Centipede. Built to be portable (runs in any modern browser on macOS, Windows, Linux) and extendable via a tunable config and feature-flag system.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
